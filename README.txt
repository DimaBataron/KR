# KR
С, Tasks &amp;&amp; Examples, test git-cmd.
Избранные задания из учебник K&R и примеры
Файлы
 1.с - Программа считающая день года. На входе: день, месяц, год.
struct.c - Тоже со структурами.
array_struct- массив структур
Cread(НЕРАБОТАЕТ) - ошибка не сдвигает адрес массива внутри структуры
Сread2 - программа выдачи перекрестных ссылок
struct_ref - записывает в бинарное дерево
Cread3 - формирует бинарное дерево. Формирует по дереву список,
сортирует этот список в завсимости от частоты. Выводит результат
Otkrf  - принимает на вход текст без запятых и верхних регистров
разбивает слово по слогам
hash - использует хэш таблицы для поиска определений ( элемент препроцессора)
hash1 -удаляет запись из хэш таблицы(полностью ли удаляет)


Заметки
1.Для отображения комментариев к программам в git, в Visual необходимо
выставить кодировку UTF-8; Файл->Дополнительные парамтры сохранения;
Для добавления аргументов компиляции
Свойства проекта->Cвойства конфигурации->Отладка->Аргументы Команд
2.Поля структуры не инициализируются по умолчанию;
3. Статическая переменная внутри функции означает что данные
не стираются после вызова функции
4. fgetc() возврашает -1 для символа 'я' этот код совпадает с
кодом EOF. Что делать?
5. Использование бестиповых элементов (Примеры)

Интересно:(asm)
1.Как инициализируются в памяти структуры
2.Как происходит доступ к полям структуры
3.Как выглядят ссылки
4.Статические переменные
5.Открытие файлов
6. Чем отличаются char и const char *const char""в памятиж
7. Как удаляеться запись из памяти;